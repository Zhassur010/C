#include <stdio.h>
#include <math.h>


int main(){

    int x=6, y=10, r;
    if(pow(r,2)<(pow(x,2)+pow(y,2))){
        printf("vhodit\n");
    }else{
        printf("ne popadayet\n");
    }

    return 0;
}