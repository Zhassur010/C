#include <math.h>

// 2 slash u kommentarry


int main(){

    double x=3, y=4, a=2, f;


    f = sqrt(pow(x, 4)+1-pow(y, 2)/pow(a, 5)+sqrt(pow(y, 2)-sqrt(x+1)));
    printf('answer: %lf\n', f);


    return 0; 
}