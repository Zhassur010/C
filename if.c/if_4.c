#include <stdio.h>
#include <math.h>

int main(){

    int a=4, b=6, c=8, nechta;
    if(a>nechta) nechta++;
    if(b>nechta) nechta++;
    if(c>nechta) nechta++;

    printf("oraside:%d\n", nechta);

    return 0;
}