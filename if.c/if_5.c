#include <stdio.h>
#include <math.h>

int main(){

    int a=5, b=-6, c=7, natija, nat;
    if(a>natija) natija++;
    if(b>natija) natija++;
    if(c>natija) natija++;
    if(a<nat) nat++;
    if(b<nat) nat++;
    if(c<nat) nat++;  

    printf("orasida:%d\n", natija); 
    printf("manfiy:%d\n", nat); 

return 0;
}