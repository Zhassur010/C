#include <stdio.h>
#include <math.h>


int main(){


    //  U = e^y + 7,355k^2 + sin^2 x

    double e=2, y=2, k=3, x=4, U;

    U = pow(e, y) + 7,355, pow,(k, 2) + pow,(sin, 2)*x;

    printf("answer: %lf\n", U);



return 0;           
}