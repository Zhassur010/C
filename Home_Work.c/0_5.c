#include <stdio.h>
#include <math.h>


int main(){

    // L = 1,51 cos x^2 + 2x^3

    double x=2, L;

    L = 1,51, (cos(pow(x, 2))) + 2,(pow(x,3));

    printf("answer: %lf/n", L);


return 0;    
}