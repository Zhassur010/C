#include <stdio.h>
#include <math.h>


int main(){

    //  D = 9,8a^2 + 5,53 cost^5

    double a=4, t=5, D;

    D = 9,8 * (pow(a,2)) + 5,53,(cos(pow(t, 5)));

    printf("answer: %lf/n", D);

return 0;    
}