#include <stdio.h>
#include <math.h>

int main(){
 
    // K = 1n(p^2 + y^3) + e^p

    double n=4, p=5, y=3, e=2, K;

    K = 1*n + (pow,(p, 2+y,3)) + (pow(e, p));

    printf("answer: %lf/n", K);

return 0;     
}